// pika() 是全域函式，不需要另外匯入

const compactCard = pika({
	padding: '16px',
	paddingLeft: '8px',
})

const spaciousCard = pika({
	padding: '24px',
	paddingLeft: '8px',
})

export { compactCard, spaciousCard }